{
  "name": "grammar-pal-polish",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for Grammar Pal's polish-to-native (Gemini) feature.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "typescript": "5.3.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.90.0"
  }
}
